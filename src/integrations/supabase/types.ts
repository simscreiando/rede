// ATENÇÃO: este arquivo é um placeholder.
// Assim que as migrations forem aplicadas a um projeto Supabase real, gere
// os tipos definitivos com:
//
//   supabase gen types typescript --project-id <PROJECT_ID> > src/integrations/supabase/types.ts
//
// Não escrevi esse comando aqui porque este sandbox não tem acesso de rede
// para se conectar a um projeto Supabase real. Até lá, o tipo abaixo é
// deliberadamente permissivo para não travar o build/typecheck do resto do
// projeto — ele não substitui os tipos gerados.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
